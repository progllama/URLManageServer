version: "3.7"

services:
  app:
    tty: true
    build:
      context: .
      dockerfile: ./docker-app-dev/Dockerfile
    command: ""
    working_dir: /go/src/app
    volumes:
      - ./src:/go/src/app